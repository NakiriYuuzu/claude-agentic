{
    "name": "claude-agentic-workspace",
    "version": "0.5.0",
    "private": true,
    "type": "module",
    "description": "Claude Agent SDK + Elysia.js + SQLite API Platform - Monorepo",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "bun run build:shared && bun run build:backend",
        "build:shared": "cd packages/shared && bun run build",
        "build:backend": "cd packages/backend && bun run build",
        "dev": "bun run build:shared && cd packages/backend && bun run dev",
        "start": "cd packages/backend && bun run start",
        "test": "bun test",
        "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "bun-types": "latest"
    }
}
